package models.view.common

import models.view.Question

class ClaimDate extends Question {
  val label: String = "Claim Date"
}

class Postcode extends Question {
  val label: String = "PostCode"
}

class FirstName extends Question {
  val label: String = "First Name"
}
